# Campuslands API REST de notas

Ejercicio guiado de 40 minutos para reforzar Node.js, Express, DTO, validaciones, arquitectura MVC, MongoDB, cookies y SemVer.

El proyecto se construye progresivamente mediante ramas de funcionalidad y se integra primero en `dev` y después en `main`.
